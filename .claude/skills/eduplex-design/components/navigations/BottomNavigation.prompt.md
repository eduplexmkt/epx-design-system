```jsx
<BottomNavigation value={tab} onChange={setTab} items={[{ value: 'home', icon: <Icon name="home" />, label: '홈' }]} />
```

Exactly 56px tall with a 1px `line.normal.alternative` top border and the same 88% frosted surface as TopNavigation. Icons are 24px; inactive is `interaction.inactive` (#989BA2), active is `primary.normal`.
