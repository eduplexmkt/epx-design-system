```jsx
<Switch checked onChange={toggle} />
<Switch size="small" />
```

52x32 (medium, 24px thumb) or 39x24 (small, 18px thumb). Off is `fill.strong`, on is `primary.normal`; the thumb is always static white. Motion uses the 200ms standard curve.
