```jsx
<Checkbox checked label="이용약관에 동의합니다" />
<Checkbox size="small" indeterminate label="일부 선택" />
```

24px (medium, body2 label) or 20px (small, label1 label). Checked fills with primary and keeps the 1.5px ring; disabled drops the whole control to 43% opacity rather than recoloring it.
