```jsx
<RadioGroup value={v} onChange={setV} options={[{ value: 'all', label: '전체' }, { value: 'new', label: '신규' }]} />
```

Same 24/20px box as Checkbox but fully round; selection is drawn as a 6px inset primary ring rather than a nested dot.
