```jsx
<Select value={v} onChange={setV} options={[{ value: 'seoul', label: '서울' }]} placeholder="지역" />
```

Trigger mirrors TextField exactly (12px radius, xsmall elevation, 12px padding, body1). The list floats 4px below on the elevated surface with medium elevation; the selected row is 5% primary with a bold primary label.
