```jsx
<Divider />
<Divider vertical size={16} color="var(--semantic-line-normal-normal)" />
<Divider thickness={8} color="var(--semantic-background-normal-alternative)" />
```

Three weights in practice: 1px `line.normal.alternative` between list rows, 1px `line.normal.normal` for stronger breaks, and a thick 8px `background.normal.alternative` band between page sections.
