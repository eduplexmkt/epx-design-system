```jsx
<Skeleton variant="rectangle" width="100%" height={180} />
<Skeleton variant="text" width="60%" />
<Skeleton variant="circle" width={40} height={40} />
```

Text rows are 22px tall with a 3px radius and 2px vertical padding; rectangles default to a 12px radius. The pulse runs 0.5 → 1 → 0.5 opacity over 2s.
